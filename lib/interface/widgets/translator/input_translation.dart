import 'package:flutter/material.dart';

class InputTranslation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(
      child: Container(
        height: 150,
        width: 300,
      ),
    );
  }
}
